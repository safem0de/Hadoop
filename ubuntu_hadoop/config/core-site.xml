<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Set the default filesystem for Hadoop -->
    <property>
        <name>fs.defaultFS</name>
        <value>hdfs://namenode:9000</value>
    </property>

    <!-- Set the Hadoop temporary directory for operations -->
    <property>
        <name>hadoop.tmp.dir</name>
        <value>/tmp/hadoop</value>
    </property>

    <!-- Set the URI for the Hadoop file system -->
    <property>
        <name>fs.hdfs.impl</name>
        <value>org.apache.hadoop.hdfs.DistributedFileSystem</value>
    </property>

    <!-- Enable Kerberos Authentication (if you're using Kerberos) -->
    <property>
        <name>hadoop.security.authentication</name>
        <value>simple</value>
    </property>

    <!-- Set the URI for the Hadoop Web UI -->
    <property>
        <name>hadoop.http.staticuser.user</name>
        <value>hadoop</value>
    </property>

    <!-- Set the default replication factor for HDFS -->
    <property>
        <name>dfs.replication</name>
        <value>1</value>
    </property>
</configuration>
